# Do NOT pin transformers here. The HF Inference base image already ships a
# transformers that supports MusicGen and is mutually compatible with the
# toolkit's sentence_transformers — pinning an old version (e.g. 4.31.0)
# breaks toolkit boot with `cannot import name 'is_torch_npu_available'`.
# The only dependency our handler.py actually adds is scipy (wavfile write).
scipy
